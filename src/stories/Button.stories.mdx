import { Meta, Canvas, Story } from "@storybook/addon-docs";
import CustomTemplate from "./customTemplate";
import Button from "../composite/Button";

<Meta title="Composite/Button" />

export const Template = CustomTemplate(Button);

# Button

## Variants

#### Primary

<Canvas>
  <Story
    name="primary"
    args={{
      label: "I'm a button",
      onClick: () => {},
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```js
export const MyComponent = (props) => {
  const handleClick = () => {
    // do something
  };
  return (
    <Button onClick={() => handleClick()} variant="primary">
      Open
    </Button>
  );
};
```

#### Primary Light

<Canvas>
  <Story
    name="primary light"
    args={{
      label: "I'm a button",
      onClick: () => {},
      variant: "primary_light",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```js
export const MyComponent = (props) => {
  const handleClick = () => {
    // do something
  };
  return (
    <Button onClick={() => handleClick()} variant="secondary">
      Open
    </Button>
  );
};
```

### Secondary

<Canvas>
  <Story
    name="secondary"
    args={{
      label: "I'm a button",
      onClick: () => {},
      variant: "secondary",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```js
export const MyComponent = (props) => {
  const handleClick = () => {
    // do something
  };
  return (
    <Button onClick={() => handleClick()} variant="secondary">
      Open
    </Button>
  );
};
```
